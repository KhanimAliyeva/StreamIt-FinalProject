@using STREAMIT.MVC.Areas.Admin.ViewModels
@model PagedMembershipViewModel

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Memberships";
    var startIndex = (Model.CurrentPage > 0 ? (Model.CurrentPage - 1) * Model.PageSize : 0);
}

<div class="content-inner container-fluid pb-0" id="page_layout">
    <div class="row">
        <div class="col-sm-12">
            <div class="streamit-wraper-table">

                <div class="card-header d-flex justify-content-between gap-3 flex-wrap align-items-center mb-4">
                    <h2 class="episode-playlist-title wp-heading-inline">
                        <span class="position-relative">Memberships</span>
                    </h2>

                    <div class="d-flex align-items-center gap-3 flex-wrap">
                        <a asp-area="Admin"
                               asp-controller="Membership"
                               asp-action="Create"
                               class="btn btn-primary">
                            <i class="fa-solid fa-plus me-2"></i> Add Membership
                        </a>

                        <span>Total: <strong>@Model.TotalCount</strong></span>
                    </div>
                </div>

                @if (TempData["DeleteError"] != null)
                {
                    <div class="alert alert-danger mb-3">
                        @TempData["DeleteError"]
                    </div>
                }

                <div class="table-view table-space streamit-wraper-table3">
                    <table class="data-tables table custom-table movie_table custom-table-height">
                        <thead>
                            <tr class="text-uppercase">
                                <th>#</th>
                                <th>Name</th>
                                <th>Price</th>
                                <th>Duration</th>
                                <th>Quality</th>
                                <th>Devices</th>
                                <th>Ads</th>
                                <th>Download</th>
                                <th>Active</th>
                                <th>Priority</th>
                                <th>Actions</th>
                            </tr>
                        </thead>

                        <tbody>
                            @if (Model.Memberships != null && Model.Memberships.Any())
                            {
                                @foreach (var item in Model.Memberships.Select((value, index) => new { value, index }))
                                {
                                    <tr>
                                        <td>@(startIndex + item.index + 1)</td>
                                        <td>@item.value.Name</td>
                                        <td>@item.value.Price.ToString("F2")</td>
                                        <td>@item.value.DurationInDays</td>
                                        <td>@item.value.VideoQuality</td>
                                        <td>@item.value.MaxDevices</td>
                                        <td>@(item.value.HasAds ? "Yes" : "No")</td>
                                        <td>@(item.value.CanDownload ? "Yes" : "No")</td>
                                        <td>@(item.value.IsActive ? "Yes" : "No")</td>
                                        <td>@item.value.PriorityLevel</td>
                                        <td>
                                            <div class="d-flex align-items-center list-user-action gap-2">
                                                <a asp-area="Admin"
                                                       asp-controller="Membership"
                                                       asp-action="Edit"
                                                       asp-route-id="@item.value.Id"
                                                       class="btn btn-sm btn-icon btn-success-subtle rounded"
                                                       title="Edit">
                                                    <i class="ph ph-pencil-simple fs-6"></i>
                                                </a>

                                                <form asp-area="Admin"
                                                      asp-controller="Membership"
                                                      asp-action="Delete"
                                                      asp-route-id="@item.value.Id"
                                                      method="post"
                                                      class="d-inline"
                                                      onsubmit="return confirm('Delete this membership?');">
                                                    @Html.AntiForgeryToken()
                                                    <button type="submit"
                                                            class="btn btn-sm btn-icon btn-danger-subtle rounded"
                                                            title="Delete">
                                                        <i class="ph ph-trash-simple fs-6"></i>
                                                    </button>
                                                </form>
                                            </div>
                                        </td>
                                    </tr>
                                }
                            }
                            else
                            {
                                <tr>
                                    <td colspan="11" class="text-center">No memberships found.</td>
                                </tr>
                            }
                        </tbody>
                    </table>

                    @if (Model.TotalPages > 1)
                    {
                        <nav aria-label="Page navigation" class="mt-3">
                            <ul class="pagination">
                                <li class="page-item @(Model.CurrentPage <= 1 ? "disabled" : "")">
                                    <a class="page-link"
                                           asp-area="Admin"
                                           asp-controller="Membership"
                                           asp-action="Index"
                                           asp-route-page="@(Model.CurrentPage - 1)">
                                        Previous
                                    </a>
                                </li>

                                @for (int i = 1; i <= Model.TotalPages; i++)
                                {
                                    <li class="page-item @(i == Model.CurrentPage ? "active" : "")">
                                        <a class="page-link"
                                               asp-area="Admin"
                                               asp-controller="Membership"
                                               asp-action="Index"
                                               asp-route-page="@i">
                                            @i
                                        </a>
                                    </li>
                                }

                                <li class="page-item @(Model.CurrentPage >= Model.TotalPages ? "disabled" : "")">
                                    <a class="page-link"
                                           asp-area="Admin"
                                           asp-controller="Membership"
                                           asp-action="Index"
                                           asp-route-page="@(Model.CurrentPage + 1)">
                                        Next
                                    </a>
                                </li>
                            </ul>
                        </nav>
                    }
                </div>

                @if (ViewBag.DebugStatusCode != null)
                {
                    <div class="mt-3 alert alert-warning">
                        <strong>API Debug:</strong> Status Code: @ViewBag.DebugStatusCode
                        <pre class="mt-2">@ViewBag.RawResponse</pre>
                    </div>
                }
            </div>
        </div>
    </div>
</div>