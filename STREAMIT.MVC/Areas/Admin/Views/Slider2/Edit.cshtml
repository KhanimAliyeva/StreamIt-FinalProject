@model STREAMIT.Business.Dtos.SliderDtos.UpdateSlider2Dto
@using STREAMIT.Business.Dtos.MovieDtos

@{
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    ViewData["Title"] = "Edit Slider";
    var movies = ViewBag.Movies as List<GetMovieDto>;
}

<div class="container-fluid py-4">
    <h3 class="mb-4">Edit Slider2</h3>

    <div class="card">
        <div class="card-body">
            <form asp-action="Edit" method="post">

                <input type="hidden" asp-for="Id" />

                <div class="mb-3">
                    <label class="form-label">Movie</label>
                    <select asp-for="MovieId" class="form-select">
                        @foreach (var movie in movies!)
                        {
                            <option value="@movie.Id"
                                    selected="@(movie.Id == Model.MovieId)">
                                @movie.Title
                            </option>
                        }
                    </select>
                </div>

                <div class="mb-3">
                    <label asp-for="Order" class="form-label"></label>
                    <input asp-for="Order" class="form-control" />
                </div>

                <div class="form-check mb-3">
                    <input asp-for="IsActive" class="form-check-input" />
                    <label asp-for="IsActive" class="form-check-label"></label>
                </div>

                <button type="submit" class="btn btn-primary">
                    Update
                </button>

                <a asp-action="Index" class="btn btn-secondary ms-2">
                    Cancel
                </a>
            </form>
        </div>
    </div>
</div>